# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build & Dev Commands

<!-- Add commands here once the project is scaffolded, e.g.:
- `npm run dev` — start dev server
- `npm run build` — production build
- `npm run lint` — lint
- `npm test` — run all tests
- `npm test -- --testPathPattern=foo` — run a single test file
-->

## Architecture

<!-- Describe the high-level structure once code exists, e.g.:
- What the project does
- Frontend/backend split, key directories
- Data flow / state management approach
- Notable non-obvious conventions
-->
